import { ApiPromise } from "@polkadot/api";
import { KeyringPair } from '@polkadot/keyring/types';
export declare const saveAlgo: (api: ApiPromise, account: KeyringPair, schemaHashes: string[], code: number[]) => Promise<number>;
export declare const runAlgo: (api: ApiPromise, issuerHash: string, account: KeyringPair, userId: string, algorithmId: number) => Promise<number>;
