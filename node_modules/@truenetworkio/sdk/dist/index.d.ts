import { ApiPromise } from "@polkadot/api";
import { KeyringPair } from '@polkadot/keyring/types';
import { NetworkConfig } from "./network";
export declare class TrueApi {
    network: ApiPromise;
    account: KeyringPair;
    issuerHash: string | undefined;
    constructor(api: ApiPromise, accountKey: string, issuerHash?: string);
    close(): Promise<void>;
    static create(accountKey: string, nodeUrl?: NetworkConfig): Promise<TrueApi>;
    setIssuer(hash: string): Promise<void>;
    registerIssuer(name: string, controllers: string[]): Promise<string>;
}
export * from './schemas';
export * from './utils';
export * from './network';
